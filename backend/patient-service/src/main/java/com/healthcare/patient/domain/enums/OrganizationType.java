package com.healthcare.patient.domain.enums;

/** Maps to patient.organization_type PostgreSQL enum (FHIR R4). */
public enum OrganizationType {
    prov, dept, team, govt, ins, pay, edu, reli, crs, cg, bus, other
}
